apiversion: v1
kind: Service
metadata:
  name: postgresql
  labels:
    app: postgresql
    role: node
spec:
  ports:
    - name: database-port
      port: 5432
      targetPort: 5432
    - name: patroni-api
      port: 8001
      targetPort: 8001
  selector:
    app: postgresql
    role: node
